/**
 * 自定义字体插件 - 前端工具脚本
 * 
 * 注意：此插件通过替换面板字体文件的方式实现字体更换
 * 不需要额外加载CSS文件
 */

var customfont_util = {
    // 检查当前是否有自定义字体应用
    check_custom_font: function() {
        // 可以通过AJAX请求检查当前字体状态
        // 此方法保留供其他页面调用
    }
};